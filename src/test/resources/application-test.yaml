spring:
  datasource:
    url: jdbc:tc:postgresql:16:///paymentdb   # starts a Postgres 16 container automatically
    username: test
    password: test
    # If your setup complains, also add:
    # driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: false